version: 0.2

phases:
  pre_build:

     commands:
       - npm install
  build:
    commands:
      - npm run webpack
artifacts:
   files:
     -index.html
     -favicon.ico
     -styles/main.css
     -Shivali_Singh_Website.pdf
     -images/*
     -dist/*
